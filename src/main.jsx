import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import "./index.scss";
import App from "./App.jsx";
import "bootstrap/dist/css/bootstrap.min.css";
import "bootstrap/dist/js/bootstrap.bundle.min.js";

createRoot(document.getElementById("root")).render(
  <StrictMode>
    <App />
  </StrictMode>
);

{
  /* Icons = font, lucide, hero icons, remix, tabler, phasphor, fether, metrial ui, iconer,  */
}
{
  /* hooks where introduce to allow developers to use state other react futures and functional components without needing to convert them into class components */
}
{
  /* context apis provide a way to pass data through the component free without passing props manual at every level. it's usefull for global data like user authentication theme preference or language settings */
}
